# Development Dockerfile for PlatePilot Frontend
# Uses Bun + Vite dev server with HMR

FROM oven/bun:1-alpine

# Install wget for healthchecks
RUN apk add --no-cache wget

WORKDIR /app

# Copy package files and config needed for quasar prepare (postinstall)
COPY package.json bun.lock* ./
COPY quasar.config.ts tsconfig.json index.html ./

# Install dependencies
RUN bun install

# Source code will be mounted as volume
# Quasar dev server will handle hot reloading

# Expose Vite dev server port
EXPOSE 9000
